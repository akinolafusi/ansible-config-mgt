pipeline {
  agent any

  stages {
    stage("scm checkout") {
      steps {
        git branch: 'main', url: 'https://github.com/akinolafusi/ansible-config-mgt.git'
      }
    }

    stage("run ansible playbook") {
      steps {
        
      }
    }
  }
}